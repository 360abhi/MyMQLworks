Mql5 notes

** int highestbarIndex  = iHighest(_Symbol,PERIOD_D1,MODE_HIGH,20,1);  //3 index
 ==>here 20 is the last 20 bars and 1 is the shift value starting from 1 prev bar
    as 0 is the current bar, it will return the index of the highest high bar

** double high  = iHigh(_Symbol,PERIOD_D1,highest); //price
--> it will return the high of the highest candle index bar

input ENUM_TIMEFRAME time = PERIOD_D1
input shiftbars = 12
==> these will ask user input 